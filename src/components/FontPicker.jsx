import { useFont } from "./FontContext"

const FontPicker = () => {
    const { font, changeFont } = useFont()

    return(
        <div>
            
        </div>
    )
}

export default FontPicker